const auth = {
    login: '/auth/login',
    logout: 'auth/logout',
};

const routes = {
    dashboard: '/',
    supplierManage: '/supplier-manage',
    sessions: '/sessions',
    auth,
};

export default routes;
